{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://raw.githubusercontent.com/api-evangelist/nyc/main/dvs/schemas/_common.json",
  "title": "NYC Veterans' Services (DVS) — Shared Definitions",
  "description": "Reusable JSON Schema definitions shared across NYC Department of Veterans' Services (DVS) object schemas. Reconciles fields common to the DVS Open Data datasets (data.cityofnewyork.us), nyc.gov/site/veterans, and the VetConnectNYC care-coordination intake (Combined Arms). Includes the NYC geography spine, DVS's own resource key (DVS_RES_ID), and veteran-specific vocabularies (military branch, discharge status, service era, service category).",
  "$defs": {
    "Borough": {
      "type": "string",
      "enum": ["Manhattan", "Bronx", "Brooklyn", "Queens", "Staten Island"]
    },
    "Coordinates": {
      "type": "object",
      "description": "WGS84 point (Open Data Latitude / Longitude).",
      "properties": {
        "latitude": { "type": "number", "minimum": -90, "maximum": 90 },
        "longitude": { "type": "number", "minimum": -180, "maximum": 180 }
      },
      "required": ["latitude", "longitude"],
      "additionalProperties": false
    },
    "Geography": {
      "type": "object",
      "description": "The NYC geography spine carried by the geocoded DVS directories (chiefly NYC Veteran Owned Businesses) — the join keys back to the rest of city government.",
      "properties": {
        "borough": { "$ref": "#/$defs/Borough" },
        "communityBoard": { "type": ["string", "integer", "null"], "description": "Community District / Board ('Community Board' / 'Community Districts')." },
        "councilDistrict": { "type": ["integer", "null"], "minimum": 1, "maximum": 51, "description": "NYC Council district ('Council District' / 'City Council Districts')." },
        "censusTract": { "type": ["string", "null"], "description": "Census tract ('Census Tract')." },
        "nta": { "type": ["string", "null"], "description": "Neighborhood Tabulation Area ('NTA')." },
        "bbl": { "type": ["string", "null"], "description": "Borough-Block-Lot ('BBL')." },
        "bin": { "type": ["string", "null"], "description": "Building Identification Number ('BIN')." },
        "policePrecinct": { "type": ["string", "integer", "null"], "description": "Police precinct ('Police Precincts')." }
      },
      "additionalProperties": false
    },
    "MilitaryBranch": {
      "type": ["string", "null"],
      "description": "Branch of military service ('Branch' / 'Branch of Service' / 'Military Affiliation').",
      "enum": ["Army", "Navy", "Air Force", "Marine Corps", "Coast Guard", "Space Force", "National Guard", "Reserves", "Other", "Unknown", null]
    },
    "DischargeStatus": {
      "type": ["string", "null"],
      "description": "Character of discharge ('Discharge Status' / 'Discharge Type' / 'Character of Discharge').",
      "enum": ["Honorable", "General (Under Honorable Conditions)", "Other Than Honorable", "Bad Conduct", "Dishonorable", "Uncharacterized", "Still Serving", "Unknown", null]
    },
    "ServiceEra": {
      "type": ["string", "null"],
      "description": "Period-of-service era ('Service Era').",
      "enum": ["Post-9/11", "Gulf War", "Vietnam Era", "Korean War", "World War II", "Peacetime", "Other", "Unknown", null]
    },
    "ServiceCategory": {
      "type": ["string", "null"],
      "description": "DVS service vertical, from the nyc.gov/site/veterans/services navigation and the assistance-request / case service types.",
      "enum": [
        "Housing",
        "Income Support / Benefits",
        "VA Claims",
        "Health",
        "Employment",
        "Education",
        "Food Assistance",
        "Legal Support",
        "Disabilities Support",
        "Military Families & Caregivers",
        "Senior Veterans",
        "Women Veterans",
        "Other"
      ]
    },
    "ResourceReference": {
      "type": "object",
      "description": "A reference to a DVS-catalogued resource or veteran-owned business by its DVS resource key.",
      "properties": {
        "dvsResId": { "type": ["string", "null"], "description": "DVS resource identifier ('DVS_RES_ID') — DVS's primary key for Resource Map entries and veteran-owned businesses." },
        "name": { "type": ["string", "null"], "description": "Resource / business name ('Name')." }
      },
      "additionalProperties": false
    }
  }
}
