{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://raw.githubusercontent.com/api-evangelist/nyc/main/doi/schemas/_common.json",
  "title": "DOI — Shared Definitions",
  "description": "Reusable JSON Schema definitions shared across NYC Department of Investigation (DOI) object schemas. Reconciles fields common to the DOI Open Data datasets (data.cityofnewyork.us — evictions, City Marshals revenue, the Policy & Procedure Recommendations portal, monthly performance indicators), nyc.gov/site/doi, and the Kaseware public complaint intake portal (app.kaseware.us). Includes the NYC geography spine carried by the Evictions dataset.",
  "$defs": {
    "Borough": {
      "type": "string",
      "enum": ["Manhattan", "Bronx", "Brooklyn", "Queens", "Staten Island"]
    },
    "Coordinates": {
      "type": "object",
      "description": "WGS84 point (Open Data LATITUDE / LONGITUDE).",
      "properties": {
        "latitude": { "type": "number", "minimum": -90, "maximum": 90 },
        "longitude": { "type": "number", "minimum": -180, "maximum": 180 }
      },
      "required": ["latitude", "longitude"],
      "additionalProperties": false
    },
    "Geography": {
      "type": "object",
      "description": "The NYC geography spine shared across DOI location-bearing datasets (chiefly the Evictions dataset) — the join keys back to the rest of city government.",
      "properties": {
        "borough": { "$ref": "#/$defs/Borough" },
        "communityBoard": { "type": ["string", "integer", "null"], "description": "Community District / Board ('Community Board')." },
        "councilDistrict": { "type": ["integer", "null"], "minimum": 1, "maximum": 51, "description": "NYC Council district ('Council District')." },
        "censusTract": { "type": ["string", "null"], "description": "Census tract ('Census Tract')." },
        "nta": { "type": ["string", "null"], "description": "Neighborhood Tabulation Area name or code ('NTA')." },
        "bbl": { "type": ["string", "null"], "description": "Borough-Block-Lot ('BBL')." },
        "bin": { "type": ["string", "null"], "description": "Building Identification Number ('BIN')." },
        "communityDistrict": { "type": ["string", "integer", "null"], "description": "Alias of communityBoard for datasets using 'COMMUNITY DISTRICT'." }
      },
      "additionalProperties": false
    },
    "MarshalReference": {
      "type": "object",
      "description": "A reference to a NYC City Marshal — the independent public officials appointed by the Mayor and overseen by DOI, whose enforcement activity (evictions) and revenue DOI publishes.",
      "properties": {
        "firstName": { "type": ["string", "null"], "description": "'Marshal First Name'." },
        "lastName": { "type": "string", "description": "'Marshal Last Name'." }
      },
      "required": ["lastName"],
      "additionalProperties": false
    },
    "AcceptanceStatus": {
      "type": ["string", "null"],
      "description": "Whether an agency accepted a DOI Policy & Procedure Recommendation ('Acceptance Status').",
      "enum": ["Accepted", "Partially Accepted", "Rejected", "Under Review", "No Response", null]
    },
    "ImplementationStatus": {
      "type": ["string", "null"],
      "description": "How far an accepted DOI recommendation has been implemented ('Implementation Status').",
      "enum": ["Implemented", "In Progress", "Not Implemented", "Partially Implemented", "Pending", null]
    }
  }
}
