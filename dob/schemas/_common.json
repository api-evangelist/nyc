{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://raw.githubusercontent.com/api-evangelist/nyc/main/dob/schemas/_common.json",
  "title": "NYC DOB \u2014 Shared Definitions",
  "description": "Reusable JSON Schema definitions shared across NYC Department of Buildings (DOB) object schemas. Reconciles fields common to the DOB website (www.nyc.gov/site/buildings), the legacy application layer (BIS Web / DOB NOW / eFiling), and the DOB NYC Open Data datasets. The GeographySpine is the recurring property-location key set that stitches every DOB record together.",
  "x-nyc-commons": {
    "role": "consumer",
    "source": "https://raw.githubusercontent.com/api-evangelist/nyc/main/nyc-commons/nyc-commons.json",
    "version": "0.1.0",
    "note": "Geography/identifier/address/money definitions are $ref'd from the canonical nyc-commons set; agency-specific definitions remain local."
  },
  "$defs": {
    "Borough": {
      "$ref": "https://raw.githubusercontent.com/api-evangelist/nyc/main/nyc-commons/geography.json#/$defs/Borough",
      "$comment": "Canonical definition provided by nyc-commons (geography.json#/$defs/Borough); migrated from a local definition. Back-compatible: the $defs name is unchanged, so object schemas referencing _common.json#/$defs/Borough still resolve."
    },
    "BoroughCode": {
      "$ref": "https://raw.githubusercontent.com/api-evangelist/nyc/main/nyc-commons/geography.json#/$defs/BoroCode",
      "$comment": "Canonical definition provided by nyc-commons (geography.json#/$defs/BoroCode); migrated from a local definition. Back-compatible: the $defs name is unchanged, so object schemas referencing _common.json#/$defs/BoroughCode still resolve."
    },
    "Coordinates": {
      "$ref": "https://raw.githubusercontent.com/api-evangelist/nyc/main/nyc-commons/geography.json#/$defs/Coordinates",
      "$comment": "Canonical definition provided by nyc-commons (geography.json#/$defs/Coordinates); migrated from a local definition. Back-compatible: the $defs name is unchanged, so object schemas referencing _common.json#/$defs/Coordinates still resolve."
    },
    "GeographySpine": {
      "$ref": "https://raw.githubusercontent.com/api-evangelist/nyc/main/nyc-commons/geography.json#/$defs/GeographySpine",
      "$comment": "Canonical definition provided by nyc-commons (geography.json#/$defs/GeographySpine); migrated from a local definition. Back-compatible: the $defs name is unchanged, so object schemas referencing _common.json#/$defs/GeographySpine still resolve."
    },
    "Address": {
      "$ref": "https://raw.githubusercontent.com/api-evangelist/nyc/main/nyc-commons/address.json#/$defs/Address",
      "$comment": "Canonical definition provided by nyc-commons (address.json#/$defs/Address); migrated from a local definition. Back-compatible: the $defs name is unchanged, so object schemas referencing _common.json#/$defs/Address still resolve."
    },
    "PartyReference": {
      "type": "object",
      "description": "A person or business on a filing/permit \u2014 owner, applicant, permittee, or filing representative. DOB decomposes these into many first/last/business-name columns.",
      "properties": {
        "role": {
          "type": [
            "string",
            "null"
          ],
          "enum": [
            "Owner",
            "Applicant",
            "Permittee",
            "FilingRepresentative",
            "SiteSafetyManager",
            "Superintendent",
            null
          ]
        },
        "firstName": {
          "type": [
            "string",
            "null"
          ]
        },
        "lastName": {
          "type": [
            "string",
            "null"
          ]
        },
        "businessName": {
          "type": [
            "string",
            "null"
          ]
        },
        "licenseType": {
          "type": [
            "string",
            "null"
          ],
          "description": "e.g. GC (General Contractor), PE, RA, Master Plumber (Open Data 'Permittee's License Type')."
        },
        "licenseNumber": {
          "type": [
            "string",
            "null"
          ],
          "description": "Open Data 'Applicant License #' / 'Permittee's License #'."
        }
      },
      "additionalProperties": false
    }
  }
}
