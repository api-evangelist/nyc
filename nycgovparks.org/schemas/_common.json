{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://raw.githubusercontent.com/api-evangelist/nyc/main/nycgovparks.org/schemas/_common.json",
  "title": "NYC Parks \u2014 Shared Definitions",
  "description": "Reusable JSON Schema definitions shared across NYC Parks object schemas. Reconciles fields common to the nycgovparks.org website and the DPR NYC Open Data datasets.",
  "x-nyc-commons": {
    "role": "consumer",
    "source": "https://raw.githubusercontent.com/api-evangelist/nyc/main/nyc-commons/nyc-commons.json",
    "version": "0.1.0",
    "note": "Geography/identifier/address/money definitions are $ref'd from the canonical nyc-commons set; agency-specific definitions remain local."
  },
  "$defs": {
    "Borough": {
      "$ref": "https://raw.githubusercontent.com/api-evangelist/nyc/main/nyc-commons/geography.json#/$defs/Borough",
      "$comment": "Canonical definition provided by nyc-commons (geography.json#/$defs/Borough); migrated from a local definition. Back-compatible: the $defs name is unchanged, so object schemas referencing _common.json#/$defs/Borough still resolve."
    },
    "Coordinates": {
      "$ref": "https://raw.githubusercontent.com/api-evangelist/nyc/main/nyc-commons/geography.json#/$defs/Coordinates",
      "$comment": "Canonical definition provided by nyc-commons (geography.json#/$defs/Coordinates); migrated from a local definition. Back-compatible: the $defs name is unchanged, so object schemas referencing _common.json#/$defs/Coordinates still resolve."
    },
    "AdminBoundaries": {
      "$ref": "https://raw.githubusercontent.com/api-evangelist/nyc/main/nyc-commons/geography.json#/$defs/AdminBoundaries",
      "$comment": "Canonical definition provided by nyc-commons (geography.json#/$defs/AdminBoundaries); migrated from a local definition. Back-compatible: the $defs name is unchanged, so object schemas referencing _common.json#/$defs/AdminBoundaries still resolve."
    },
    "ParkReference": {
      "type": "object",
      "description": "A reference to a Park by its GIS property number \u2014 the join key across DPR datasets (GISPROPNUM / OMPPROPID / gispropnum).",
      "properties": {
        "gisPropNum": {
          "type": "string",
          "description": "GIS property number, e.g. 'B073'."
        },
        "name": {
          "type": "string"
        }
      },
      "required": [
        "gisPropNum"
      ],
      "additionalProperties": false
    },
    "Accessibility": {
      "type": "string",
      "description": "ADA accessibility status as published on the facility directories.",
      "enum": [
        "Accessible",
        "Partially Accessible",
        "Not Accessible",
        "Unknown"
      ]
    }
  }
}
