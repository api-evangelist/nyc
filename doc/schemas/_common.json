{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://raw.githubusercontent.com/api-evangelist/nyc/main/doc/schemas/_common.json",
  "title": "DOC \u2014 Shared Definitions",
  "description": "Reusable JSON Schema definitions shared across NYC Department of Correction (DOC) object schemas. Reconciles fields common to the DOC Open Data datasets (data.cityofnewyork.us), nyc.gov/site/doc, and the legacy Inmate Lookup Service / 'P.I.C. Lookup' (a073-ils-web.nyc.gov). Includes the NYC geography spine, DOC facility references, and the shared person-in-custody demographic dimensions.",
  "x-nyc-commons": {
    "role": "consumer",
    "source": "https://raw.githubusercontent.com/api-evangelist/nyc/main/nyc-commons/nyc-commons.json",
    "version": "0.1.0",
    "note": "Geography/identifier/address/money definitions are $ref'd from the canonical nyc-commons set; agency-specific definitions remain local."
  },
  "$defs": {
    "Borough": {
      "$ref": "https://raw.githubusercontent.com/api-evangelist/nyc/main/nyc-commons/geography.json#/$defs/Borough",
      "$comment": "Canonical definition provided by nyc-commons (geography.json#/$defs/Borough); migrated from a local definition. Back-compatible: the $defs name is unchanged, so object schemas referencing _common.json#/$defs/Borough still resolve."
    },
    "Coordinates": {
      "$ref": "https://raw.githubusercontent.com/api-evangelist/nyc/main/nyc-commons/geography.json#/$defs/Coordinates",
      "$comment": "Canonical definition provided by nyc-commons (geography.json#/$defs/Coordinates); migrated from a local definition. Back-compatible: the $defs name is unchanged, so object schemas referencing _common.json#/$defs/Coordinates still resolve."
    },
    "Geography": {
      "$ref": "https://raw.githubusercontent.com/api-evangelist/nyc/main/nyc-commons/geography.json#/$defs/GeographySpine",
      "$comment": "Canonical definition provided by nyc-commons (geography.json#/$defs/GeographySpine); migrated from a local definition. Back-compatible: the $defs name is unchanged, so object schemas referencing _common.json#/$defs/Geography still resolve."
    },
    "FacilityReference": {
      "type": "object",
      "description": "A reference to a DOC facility (jail) by its operational code and name. Facility codes appear as the 'FACILITY' dimension across DOC incident and lock-in datasets (e.g. AMKC, GRVC, RNDC, RMSC, OBCC, VCBC, EMTC, WF, NIC, MDC).",
      "properties": {
        "facility": {
          "type": "string",
          "description": "Facility code or name ('FACILITY') \u2014 e.g. 'AMKC', 'GRVC', 'RNDC'."
        },
        "facilityName": {
          "type": [
            "string",
            "null"
          ],
          "description": "Full facility name (e.g. 'Anna M. Kross Center', 'George R. Vierno Center')."
        }
      },
      "required": [
        "facility"
      ],
      "additionalProperties": false
    },
    "Demographics": {
      "type": "object",
      "description": "Person-in-custody demographic dimensions shared across the custody and incident datasets ('AGE', 'RACE', 'GENDER').",
      "properties": {
        "age": {
          "type": [
            "integer",
            "null"
          ],
          "description": "Age ('AGE')."
        },
        "race": {
          "type": [
            "string",
            "null"
          ],
          "description": "Race/ethnicity code ('RACE')."
        },
        "gender": {
          "type": [
            "string",
            "null"
          ],
          "description": "Gender ('GENDER')."
        }
      },
      "additionalProperties": false
    },
    "IncidentType": {
      "type": "string",
      "description": "DOC incident categories, unified across the parallel incident datasets.",
      "enum": [
        "Death",
        "Slashing",
        "Stabbing",
        "Inmate Fight",
        "Assault on Staff",
        "Staff Injury (Class A)",
        "Use of Force",
        "Emergency Lock-In",
        "Other"
      ]
    }
  }
}
