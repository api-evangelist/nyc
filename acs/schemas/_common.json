{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://raw.githubusercontent.com/api-evangelist/nyc/main/acs/schemas/_common.json",
  "title": "ACS — Shared Definitions",
  "description": "Reusable JSON Schema definitions shared across NYC Administration for Children's Services (ACS) object schemas. Reconciles fields common to the ACS Open Data assets (data.cityofnewyork.us), nyc.gov/site/acs, and the delegated intake channels (NYC 311 and the NY State OCFS Statewide Central Register). Includes the NYC geography spine used by the provider directory and the reporting-period spine used by ACS's aggregate accountability reports.",
  "$defs": {
    "Borough": {
      "type": "string",
      "enum": ["Manhattan", "Bronx", "Brooklyn", "Queens", "Staten Island"]
    },
    "Coordinates": {
      "type": "object",
      "description": "WGS84 point (Open Data LATITUDE / LONGITUDE).",
      "properties": {
        "latitude": { "type": "number", "minimum": -90, "maximum": 90 },
        "longitude": { "type": "number", "minimum": -180, "maximum": 180 }
      },
      "required": ["latitude", "longitude"],
      "additionalProperties": false
    },
    "Geography": {
      "type": "object",
      "description": "The NYC geography spine shared across ACS datasets that carry a location — the join keys back to the rest of city government.",
      "properties": {
        "borough": { "$ref": "#/$defs/Borough" },
        "communityBoard": { "type": ["string", "integer", "null"], "description": "Community District / Board (Open Data 'COMMUNITY BOARD' / 'Community District (CD)')." },
        "councilDistrict": { "type": ["integer", "null"], "minimum": 1, "maximum": 51, "description": "NYC Council district ('COUNCIL DISTRICT')." },
        "censusTract": { "type": ["string", "null"], "description": "Census tract ('CENSUS TRACT')." },
        "nta": { "type": ["string", "null"], "description": "Neighborhood Tabulation Area name or code ('NTA')." },
        "bbl": { "type": ["string", "null"], "description": "Borough-Block-Lot ('BBL')." },
        "bin": { "type": ["string", "null"], "description": "Building Identification Number ('BIN')." },
        "postcode": { "type": ["string", "null"], "description": "ZIP / postal code ('POSTCODE')." }
      },
      "additionalProperties": false
    },
    "ReportPeriod": {
      "type": "object",
      "description": "The reporting-period spine shared across ACS's aggregate accountability reports (Child Welfare Indicators, Monthly Flash Report, Reports to City Council). ACS publishes counts by period, not per case.",
      "properties": {
        "period": { "type": ["string", "null"], "description": "Human label of the reporting window ('Report Period' / 'Data Period' / 'Date Range of Data')." },
        "year": { "type": ["integer", "string", "null"], "description": "Calendar or fiscal year ('Calendar Year' / 'Year')." },
        "frequency": {
          "type": ["string", "null"],
          "description": "Reporting cadence ('Report Update Frequency').",
          "enum": ["Monthly", "Quarterly", "Annual", "Semiannual", "Historical", null]
        }
      },
      "additionalProperties": false
    },
    "ServiceType": {
      "type": ["string", "null"],
      "description": "The broad ACS service line an object belongs to.",
      "enum": ["Child Welfare", "Preventive Services", "Foster Care", "Youth & Family Justice", "Child Care & Head Start", "Community Partnership", null]
    }
  }
}
