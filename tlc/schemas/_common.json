{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://raw.githubusercontent.com/api-evangelist/nyc/main/tlc/schemas/_common.json",
  "title": "NYC TLC — Shared Definitions",
  "description": "Reusable JSON Schema definitions shared across NYC Taxi & Limousine Commission (TLC) object schemas. Reconciles fields common to the TLC website (nyc.gov/site/tlc), the TLC Trip Record Data files (parquet on CloudFront), and the 80 TLC datasets on NYC Open Data (Socrata). Includes the NYC geography spine and the TLC Taxi Zone reference used by trip-record location IDs.",
  "$defs": {
    "Borough": {
      "type": "string",
      "enum": ["Manhattan", "Bronx", "Brooklyn", "Queens", "Staten Island", "EWR", "Unknown"],
      "description": "NYC borough. Trip-record taxi zones also include EWR (Newark) and Unknown (LocationID 264/265)."
    },
    "LicenseType": {
      "type": "string",
      "enum": [
        "Medallion Taxi",
        "Street Hail Livery (SHL)",
        "For-Hire Vehicle (FHV)",
        "Commuter Van",
        "Paratransit",
        "FHV Base",
        "Medallion Driver",
        "FHV Driver",
        "Other"
      ],
      "description": "TLC license/endorsement category (driver, vehicle, or base)."
    },
    "ServiceType": {
      "type": "string",
      "enum": ["yellow", "green", "fhv", "fhvhv"],
      "description": "Trip-record service class: yellow (medallion), green (SHL/boro taxi), fhv (for-hire), fhvhv (high-volume for-hire, e.g. Uber/Lyft)."
    },
    "Coordinates": {
      "type": "object",
      "description": "WGS84 point.",
      "properties": {
        "latitude": { "type": "number", "minimum": -90, "maximum": 90 },
        "longitude": { "type": "number", "minimum": -180, "maximum": 180 }
      },
      "required": ["latitude", "longitude"],
      "additionalProperties": false
    },
    "Geography": {
      "type": "object",
      "description": "NYC geography spine — the administrative and statistical identifiers a TLC address or taxi zone can be joined to.",
      "properties": {
        "borough": { "$ref": "#/$defs/Borough" },
        "communityBoard": { "type": ["string", "null"], "description": "Community District / Board." },
        "councilDistrict": { "type": ["integer", "null"], "minimum": 1, "maximum": 51 },
        "censusTract": { "type": ["string", "null"] },
        "nta": { "type": ["string", "null"], "description": "Neighborhood Tabulation Area." },
        "policePrecinct": { "type": ["string", "null"] },
        "bbl": { "type": ["string", "null"], "description": "Borough-Block-Lot." },
        "bin": { "type": ["string", "null"], "description": "Building Identification Number." },
        "taxiZone": { "type": ["integer", "null"], "description": "TLC Taxi Zone LocationID (1-263) — see taxi-zone.json." },
        "zip": { "type": ["string", "null"] }
      },
      "additionalProperties": false
    },
    "TaxiZoneReference": {
      "type": "object",
      "description": "A reference to a TLC Taxi Zone by LocationID — the join key behind trip-record PULocationID / DOLocationID.",
      "properties": {
        "locationId": { "type": "integer", "minimum": 1, "maximum": 265, "description": "TLC Taxi Zone LocationID." },
        "zone": { "type": ["string", "null"], "description": "Zone name, e.g. 'JFK Airport'." },
        "borough": { "$ref": "#/$defs/Borough" }
      },
      "required": ["locationId"],
      "additionalProperties": false
    },
    "BaseReference": {
      "type": "object",
      "description": "A reference to an FHV base station that dispatched or affiliates a vehicle.",
      "properties": {
        "baseNumber": { "type": ["string", "null"], "description": "TLC base license number, e.g. 'B00256'." },
        "baseName": { "type": ["string", "null"] }
      },
      "additionalProperties": false
    }
  }
}
