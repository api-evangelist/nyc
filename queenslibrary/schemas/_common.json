{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://raw.githubusercontent.com/api-evangelist/nyc/main/queenslibrary/schemas/_common.json",
  "title": "Queens Public Library \u2014 Shared Definitions",
  "description": "Reusable JSON Schema definitions shared across Queens Public Library (QPL) object schemas. Reconciles fields common to the one QPL Open Data dataset (data.cityofnewyork.us \u2014 'Queens Library Branches', agency label 'Queens Library (QBPL)'), the queenslibrary.org Drupal site, and the packaged SaaS platforms QPL runs on: the BiblioCommons catalog (queenslibrary.bibliocommons.com), Communico events (queens.libnet.info / connect.queenslibrary.org), and the OverDrive/Libby, Axis 360, and hoopla digital-lending silos. Includes the NYC geography spine, a shared BranchReference, and a Vendor enum naming the third-party platforms that own each surface.",
  "x-nyc-commons": {
    "role": "consumer",
    "source": "https://raw.githubusercontent.com/api-evangelist/nyc/main/nyc-commons/nyc-commons.json",
    "version": "0.1.0",
    "note": "Geography/identifier/address/money definitions are $ref'd from the canonical nyc-commons set; agency-specific definitions remain local."
  },
  "$defs": {
    "Borough": {
      "$ref": "https://raw.githubusercontent.com/api-evangelist/nyc/main/nyc-commons/geography.json#/$defs/Borough",
      "$comment": "Canonical definition provided by nyc-commons (geography.json#/$defs/Borough); migrated from a local definition. Back-compatible: the $defs name is unchanged, so object schemas referencing _common.json#/$defs/Borough still resolve."
    },
    "Coordinates": {
      "$ref": "https://raw.githubusercontent.com/api-evangelist/nyc/main/nyc-commons/geography.json#/$defs/Coordinates",
      "$comment": "Canonical definition provided by nyc-commons (geography.json#/$defs/Coordinates); migrated from a local definition. Back-compatible: the $defs name is unchanged, so object schemas referencing _common.json#/$defs/Coordinates still resolve."
    },
    "Geography": {
      "$ref": "https://raw.githubusercontent.com/api-evangelist/nyc/main/nyc-commons/geography.json#/$defs/GeographySpine",
      "$comment": "Canonical definition provided by nyc-commons (geography.json#/$defs/GeographySpine); migrated from a local definition. Back-compatible: the $defs name is unchanged, so object schemas referencing _common.json#/$defs/Geography still resolve."
    },
    "BranchReference": {
      "type": "object",
      "description": "A reference to a QPL branch/location by name and identifiers.",
      "properties": {
        "branch": {
          "type": "string",
          "description": "Branch name ('Branch')."
        },
        "branchId": {
          "type": [
            "string",
            "null"
          ],
          "description": "Branch code/slug used by the site, catalog, or Communico (not published to Open Data)."
        }
      },
      "required": [
        "branch"
      ],
      "additionalProperties": false
    },
    "Vendor": {
      "type": [
        "string",
        "null"
      ],
      "description": "Third-party SaaS platform that owns the underlying surface. QPL is a nonprofit that composes its digital presence from packaged vendors; almost nothing is an owned QPL API.",
      "enum": [
        "BiblioCommons",
        "Communico",
        "OverDrive",
        "Libby",
        "Axis 360",
        "hoopla",
        "Springshare",
        "NYC Open Data",
        "Drupal",
        null
      ]
    },
    "MediaFormat": {
      "type": [
        "string",
        "null"
      ],
      "description": "Catalog / digital media format.",
      "enum": [
        "Book",
        "eBook",
        "Audiobook",
        "eAudiobook",
        "eMagazine",
        "eVideo",
        "DVD",
        "Blu-ray",
        "Music CD",
        "Large Print",
        "Graphic Novel",
        "eComic",
        "Kit",
        "Other",
        null
      ]
    }
  }
}
