{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://raw.githubusercontent.com/api-evangelist/nyc/main/coib/schemas/_common.json",
  "title": "COIB — Shared Definitions",
  "description": "Reusable JSON Schema definitions shared across NYC Conflicts of Interest Board (COIB) object schemas. Reconciles fields common to the COIB Open Data datasets (data.cityofnewyork.us), nyc.gov/site/coib, and the login-gated Annual Financial Disclosure filing website. Includes the NYC geography spine (carried only by the Legal Defense Trust Expenditures dataset) and the shared money/filer references used across enforcement, donations, and disclosure objects.",
  "$defs": {
    "Borough": {
      "type": "string",
      "enum": ["Manhattan", "Bronx", "Brooklyn", "Queens", "Staten Island"]
    },
    "Coordinates": {
      "type": "object",
      "description": "WGS84 point (Open Data Latitude / Longitude).",
      "properties": {
        "latitude": { "type": "number", "minimum": -90, "maximum": 90 },
        "longitude": { "type": "number", "minimum": -180, "maximum": 180 }
      },
      "required": ["latitude", "longitude"],
      "additionalProperties": false
    },
    "Geography": {
      "type": "object",
      "description": "The NYC geography spine — the join keys back to the rest of city government. Among COIB datasets it is carried in full only by Legal Defense Trust Expenditures.",
      "properties": {
        "borough": { "$ref": "#/$defs/Borough" },
        "communityBoard": { "type": ["string", "integer", "null"], "description": "Community District / Board ('Community Board')." },
        "councilDistrict": { "type": ["integer", "null"], "minimum": 1, "maximum": 51, "description": "NYC Council district ('Council District')." },
        "censusTract": { "type": ["string", "null"], "description": "2020 Census tract ('Census Tract (2020)')." },
        "nta": { "type": ["string", "null"], "description": "Neighborhood Tabulation Area name or code ('Neighborhood Tabulation Area (NTA) (2020)')." },
        "bbl": { "type": ["string", "null"], "description": "Borough-Block-Lot ('BBL')." },
        "bin": { "type": ["string", "null"], "description": "Building Identification Number ('BIN')." }
      },
      "additionalProperties": false
    },
    "PostalAddress": {
      "type": "object",
      "description": "A mailing address as carried by the Legal Defense Trust and donation datasets (city/state/zip; payee/donor location).",
      "properties": {
        "address": { "type": ["string", "null"], "description": "Street address ('Payee Address')." },
        "city": { "type": ["string", "null"], "description": "'City' / \"Donor's City of Residence\"." },
        "state": { "type": ["string", "null"], "description": "'State' / \"Donor's State of Residence\"." },
        "zipCode": { "type": ["string", "null"], "description": "'Zip Code' / 'Zip'." }
      },
      "additionalProperties": false
    },
    "Money": {
      "type": ["number", "null"],
      "description": "A monetary amount in US dollars."
    },
    "AgencyReference": {
      "type": "object",
      "description": "A reference to a City agency, the unit COIB regulates.",
      "properties": {
        "agencyName": { "type": ["string", "null"], "description": "Agency name ('Agency' / 'Agency Name')." }
      },
      "additionalProperties": false
    },
    "FilerReference": {
      "type": "object",
      "description": "A reference to a public servant or filer (name + title + agency). Used by the Policymakers List and the net-new Financial Disclosure filing.",
      "properties": {
        "name": { "type": ["string", "null"], "description": "Full name ('Name of Employee')." },
        "firstName": { "type": ["string", "null"], "description": "'First Name'." },
        "lastName": { "type": ["string", "null"], "description": "'Last Name'." },
        "title": { "type": ["string", "null"], "description": "Position title ('Title')." },
        "agencyName": { "type": ["string", "null"], "description": "Employing agency ('Agency Name')." }
      },
      "additionalProperties": false
    }
  }
}
