{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://raw.githubusercontent.com/api-evangelist/nyc/main/dohmh/schemas/_common.json",
  "title": "NYC Health (DOHMH) — Shared Definitions",
  "description": "Reusable JSON Schema definitions shared across NYC Department of Health and Mental Hygiene object schemas. Reconciles the geography spine and address fields that recur across the DOHMH NYC Open Data datasets (restaurant inspections 43nn-pn8j, rodent inspection p937-wjvj, childcare inspections dsg6-ifza, indoor environmental complaints 9jgj-bmct) and the DOH transactional systems (ABC Eats, eVital, Accela Citizen Access).",
  "$defs": {
    "Borough": {
      "type": "string",
      "description": "NYC borough. Open Data sets encode this variously as a name (BOROUGH), an abbreviation (BORO), or a code (BORO_CODE 1-5); this API normalizes to the full name.",
      "enum": ["Manhattan", "Bronx", "Brooklyn", "Queens", "Staten Island"]
    },
    "Coordinates": {
      "type": "object",
      "description": "WGS84 point (Open Data latitude/longitude columns).",
      "properties": {
        "latitude": { "type": "number", "minimum": -90, "maximum": 90 },
        "longitude": { "type": "number", "minimum": -180, "maximum": 180 }
      },
      "required": ["latitude", "longitude"],
      "additionalProperties": false
    },
    "GeoLocation": {
      "type": "object",
      "description": "The shared NYC geography spine appended to most DOHMH Open Data rows: community board, council district, census tract, NTA, and the tax-lot identifiers BBL/BIN. Every field is nullable because coverage varies by dataset and by row.",
      "properties": {
        "borough": { "$ref": "#/$defs/Borough" },
        "communityBoard": { "type": ["string", "null"], "description": "Community District / Board (Open Data 'Community Board')." },
        "councilDistrict": { "type": ["string", "null"], "description": "City Council District (Open Data 'Council District')." },
        "censusTract": { "type": ["string", "null"], "description": "Census Tract (Open Data 'Census Tract')." },
        "nta": { "type": ["string", "null"], "description": "Neighborhood Tabulation Area (Open Data 'NTA')." },
        "bbl": { "type": ["string", "null"], "description": "Borough-Block-Lot tax-lot id (Open Data 'BBL')." },
        "bin": { "type": ["string", "null"], "description": "Building Identification Number (Open Data 'BIN')." },
        "coordinates": { "$ref": "#/$defs/Coordinates" }
      },
      "additionalProperties": false
    },
    "Address": {
      "type": "object",
      "description": "A street address as decomposed across DOHMH datasets (building/house number, street, ZIP, borough).",
      "properties": {
        "building": { "type": ["string", "null"], "description": "Building / house number." },
        "street": { "type": ["string", "null"] },
        "zipCode": { "type": ["string", "null"] },
        "borough": { "$ref": "#/$defs/Borough" },
        "phone": { "type": ["string", "null"] }
      },
      "additionalProperties": false
    },
    "Violation": {
      "type": "object",
      "description": "A single cited violation on an inspection (restaurant, childcare, and similar inspection datasets are one-row-per-violation).",
      "properties": {
        "code": { "type": ["string", "null"], "description": "Violation code (Open Data 'violation_code')." },
        "description": { "type": ["string", "null"], "description": "Violation description (Open Data 'violation_description')." },
        "critical": { "type": ["boolean", "null"], "description": "Whether the violation is critical (derived from Open Data 'critical_flag')." }
      },
      "additionalProperties": false
    }
  }
}
