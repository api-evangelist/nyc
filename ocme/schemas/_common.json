{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://raw.githubusercontent.com/api-evangelist/nyc/main/ocme/schemas/_common.json",
  "title": "OCME — Shared Definitions",
  "description": "Reusable JSON Schema definitions shared across NYC Office of Chief Medical Examiner (OCME) object schemas. Reconciles fields common to the one OCME NYC Open Data asset (data.cityofnewyork.us), nyc.gov/site/ocme, the NYC 311 request channel, and the federal NamUs missing-persons system. Includes the NYC geography spine and a deliberately NON-IDENTIFYING case reference — OCME's core object (a death investigation) is confidential and is never modeled here at a per-decedent, clinical level of detail.",
  "$defs": {
    "Borough": {
      "type": "string",
      "enum": ["Manhattan", "Bronx", "Brooklyn", "Queens", "Staten Island"]
    },
    "Coordinates": {
      "type": "object",
      "description": "WGS84 point (Open Data LATITUDE / LONGITUDE).",
      "properties": {
        "latitude": { "type": "number", "minimum": -90, "maximum": 90 },
        "longitude": { "type": "number", "minimum": -180, "maximum": 180 }
      },
      "required": ["latitude", "longitude"],
      "additionalProperties": false
    },
    "Geography": {
      "type": "object",
      "description": "The NYC geography spine — the join keys back to the rest of city government.",
      "properties": {
        "borough": { "$ref": "#/$defs/Borough" },
        "communityBoard": { "type": ["string", "integer", "null"], "description": "Community District / Board." },
        "councilDistrict": { "type": ["integer", "null"], "minimum": 1, "maximum": 51, "description": "NYC Council district." },
        "censusTract": { "type": ["string", "null"], "description": "2020 Census tract." },
        "nta": { "type": ["string", "null"], "description": "Neighborhood Tabulation Area name or code." },
        "zipCode": { "type": ["string", "null"], "description": "ZIP / postal code." }
      },
      "additionalProperties": false
    },
    "MannerOfDeath": {
      "type": ["string", "null"],
      "description": "The five statutory manners of death an OCME investigation may certify, plus pending/undetermined. Used only in AGGREGATE statistics, never against an individual.",
      "enum": ["Natural", "Accident", "Homicide", "Suicide", "Undetermined", "Pending", null]
    },
    "CaseReference": {
      "type": "object",
      "description": "A deliberately NON-IDENTIFYING reference to an OCME death-investigation case. Carries the OCME case number and, for a records request, the minimum a requester supplies to locate a case — never clinical findings, cause of death, or investigative detail.",
      "properties": {
        "caseNumber": { "type": ["string", "null"], "description": "OCME case / accession number (ME number), if known to the requester." },
        "decedentLastName": { "type": ["string", "null"], "description": "Decedent surname, as supplied by the requester to locate the case." },
        "decedentFirstName": { "type": ["string", "null"], "description": "Decedent given name, as supplied by the requester." },
        "dateOfDeath": { "type": ["string", "null"], "format": "date", "description": "Approximate or known date of death, to disambiguate the case." },
        "borough": { "$ref": "#/$defs/Borough" }
      },
      "additionalProperties": false
    },
    "ContactPerson": {
      "type": "object",
      "description": "Contact details for a requester or point of contact.",
      "properties": {
        "name": { "type": ["string", "null"] },
        "organization": { "type": ["string", "null"] },
        "phone": { "type": ["string", "null"] },
        "email": { "type": ["string", "null"], "format": "email" }
      },
      "additionalProperties": false
    }
  }
}
