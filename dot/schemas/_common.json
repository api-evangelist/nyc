{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://raw.githubusercontent.com/api-evangelist/nyc/main/dot/schemas/_common.json",
  "title": "NYC DOT — Shared Definitions",
  "description": "Reusable JSON Schema definitions shared across NYC Department of Transportation object schemas. Reconciles fields common to nyc.gov/dot, nycstreetdesign.info (Drupal), and the DOT NYC Open Data (Socrata) datasets — most importantly the LION street SegmentReference that DOT uses to key almost every street asset.",
  "$defs": {
    "Borough": {
      "type": "string",
      "description": "NYC borough. Open Data variously stores this as a name (BoroName), a 1-char code (BoroCode: 1=Manhattan, 2=Bronx, 3=Brooklyn, 4=Queens, 5=Staten Island), or an abbreviation (Boro).",
      "enum": ["Manhattan", "Bronx", "Brooklyn", "Queens", "Staten Island"]
    },
    "Coordinates": {
      "type": "object",
      "description": "WGS84 latitude/longitude. Many DOT datasets also carry POINT_X/POINT_Y or WktGeom in NY State Plane (EPSG:2263); those should be reprojected to WGS84 here.",
      "properties": {
        "latitude": { "type": "number", "minimum": -90, "maximum": 90 },
        "longitude": { "type": "number", "minimum": -180, "maximum": 180 }
      },
      "required": ["latitude", "longitude"],
      "additionalProperties": false
    },
    "Geography": {
      "type": "object",
      "description": "The NYC geographic spine — the administrative areas a street asset falls within. In Open Data these arrive as Socrata computed_region columns (Community Districts, City Council Districts, Police Precincts, NTA) and as native fields (BoroCD, CounDist, NTAname, BIN, BBL).",
      "properties": {
        "borough": { "$ref": "#/$defs/Borough" },
        "communityBoard": { "type": ["string", "integer", "null"], "description": "Community District / Board (Open Data BoroCD / borocd)." },
        "councilDistrict": { "type": ["integer", "null"], "minimum": 1, "maximum": 51, "description": "NYC Council district (Open Data CounDist)." },
        "censusTract": { "type": ["string", "null"], "description": "US Census tract (2020)." },
        "nta": { "type": ["string", "null"], "description": "Neighborhood Tabulation Area name (Open Data NTAname)." },
        "bbl": { "type": ["string", "null"], "description": "Borough-Block-Lot parcel identifier, when a location resolves to a tax lot." },
        "bin": { "type": ["string", "null"], "description": "Building Identification Number, when a structure applies (e.g. a bridge BIN)." }
      },
      "additionalProperties": false
    },
    "SegmentReference": {
      "type": "object",
      "description": "A reference to a LION street segment — the DOT join key. Open Data carries this as SegmentID / segmentid on nearly every street asset (bike routes, traffic counts, closures), plus the on/from/to street naming used to describe a blockface.",
      "properties": {
        "segmentId": { "type": ["string", "integer", "null"], "description": "LION SegmentID (Open Data SegmentID / segmentid) — the citywide street-centerline key." },
        "onStreet": { "type": ["string", "null"], "description": "The street the asset is on (Open Data OnStreetName / on_street / street)." },
        "fromStreet": { "type": ["string", "null"], "description": "Cross street the segment runs from (FromStreetName / from_street)." },
        "toStreet": { "type": ["string", "null"], "description": "Cross street the segment runs to (ToStreetName / to_street)." },
        "sideOfStreet": { "type": ["string", "null"], "enum": ["N", "S", "E", "W", "NE", "NW", "SE", "SW", null], "description": "Side of street (Open Data side_of_street) where relevant." }
      },
      "additionalProperties": false
    },
    "GeoJSONGeometry": {
      "type": "object",
      "description": "A GeoJSON geometry. DOT street assets are overwhelmingly geospatial; Open Data stores them as the_geom (Point / MultiLineString / MultiPolygon) or WKT — normalized to GeoJSON here.",
      "properties": {
        "type": { "type": "string", "enum": ["Point", "LineString", "MultiLineString", "Polygon", "MultiPolygon"] },
        "coordinates": { "type": "array" }
      },
      "required": ["type", "coordinates"],
      "additionalProperties": false
    }
  }
}
