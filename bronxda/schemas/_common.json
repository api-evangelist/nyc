{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://raw.githubusercontent.com/api-evangelist/nyc/main/bronxda/schemas/_common.json",
  "title": "Bronx DA — Shared Definitions",
  "description": "Reusable JSON Schema definitions shared across Office of the Bronx District Attorney object schemas. Because the Bronx DA publishes no Open Data and no API, these fields are reverse-engineered from the public /html/ SHTML site (bronxda.nyc.gov) and its embedded Power BI (Gov) dashboards. Deliberately generic across all five NYC borough District Attorney offices so the model can back one shared District Attorney API.",
  "$defs": {
    "Borough": {
      "type": "string",
      "description": "The NYC borough / county the District Attorney serves. Included for the citywide geography spine; the Bronx DA is always 'Bronx' (Bronx County).",
      "enum": ["Manhattan", "Bronx", "Brooklyn", "Queens", "Staten Island"]
    },
    "County": {
      "type": "string",
      "description": "New York County name for the office (prosecution is a county-level function). Bronx DA = 'Bronx'.",
      "enum": ["New York", "Bronx", "Kings", "Queens", "Richmond"]
    },
    "Coordinates": {
      "type": "object",
      "description": "WGS84 point (latitude / longitude) for a physical location such as an office, resource site, or event venue.",
      "properties": {
        "latitude": { "type": "number", "minimum": -90, "maximum": 90 },
        "longitude": { "type": "number", "minimum": -180, "maximum": 180 }
      },
      "required": ["latitude", "longitude"],
      "additionalProperties": false
    },
    "Geography": {
      "type": "object",
      "description": "The NYC geography spine — the join keys back to the rest of city government. Sparse for a DA office (records are case- and person-centric, not parcel-centric) but carried so resources/events can be located.",
      "properties": {
        "borough": { "$ref": "#/$defs/Borough" },
        "neighborhood": { "type": ["string", "null"], "description": "Neighborhood or area name (e.g. 'The Hub', 'Mott Haven')." },
        "communityBoard": { "type": ["string", "integer", "null"], "description": "Bronx Community District / Board (1-12)." },
        "councilDistrict": { "type": ["integer", "null"], "minimum": 1, "maximum": 51, "description": "NYC Council district." },
        "policePrecinct": { "type": ["string", "integer", "null"], "description": "NYPD precinct associated with the matter/location." },
        "zipCode": { "type": ["string", "null"], "description": "ZIP code." }
      },
      "additionalProperties": false
    },
    "ContactInfo": {
      "type": "object",
      "description": "Public contact details for an office, bureau, unit, or program.",
      "properties": {
        "phone": { "type": ["string", "null"] },
        "email": { "type": ["string", "null"], "format": "email" },
        "address": { "type": ["string", "null"] },
        "url": { "type": ["string", "null"], "format": "uri" },
        "hours": { "type": ["string", "null"] }
      },
      "additionalProperties": false
    },
    "OfficeReference": {
      "type": "object",
      "description": "A reference to the prosecuting office. Present on every record so a shared District Attorney API can distinguish the five borough offices.",
      "properties": {
        "office": { "type": "string", "description": "Office name, e.g. 'Office of the Bronx District Attorney'.", "default": "Office of the Bronx District Attorney" },
        "county": { "$ref": "#/$defs/County" },
        "borough": { "$ref": "#/$defs/Borough" },
        "districtAttorney": { "type": ["string", "null"], "description": "Name of the sitting District Attorney (Bronx: 'Darcel D. Clark')." }
      },
      "required": ["office"],
      "additionalProperties": false
    },
    "Bureau": {
      "type": ["string", "null"],
      "description": "Organizational bureau / division the record belongs to, from the office's /html/bureaus/ pages.",
      "enum": [
        "Trial Division",
        "Investigations Division",
        "Special Victims Division",
        "Strategic Enforcement Division",
        "General Counsel Division",
        "Crime Victims Assistance Bureau",
        "Community Affairs Unit",
        "Immigrant Affairs Unit",
        "Civilian Complaint Unit",
        "Office of Diversity, Equity and Inclusion",
        "Equal Employment Opportunity",
        null
      ]
    }
  }
}
