{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://raw.githubusercontent.com/api-evangelist/nyc/main/hhc/schemas/_common.json",
  "title": "NYC Health + Hospitals — Shared Definitions",
  "description": "Reusable JSON Schema definitions shared across NYC Health + Hospitals (H+H) object schemas. Reconciles fields common to the H+H public site (nychealthandhospitals.org), the MyChart patient portal, and the live Epic FHIR R4 / SMART on FHIR API (epicproxypda.nychhc.org). Where a concept has a clean FHIR analogue (Location, Organization, Practitioner, Appointment) the mapping is noted in x-fhir so the design-first API can align to the standard H+H already runs.",
  "$defs": {
    "Borough": {
      "type": "string",
      "enum": ["Manhattan", "Bronx", "Brooklyn", "Queens", "Staten Island"]
    },
    "Coordinates": {
      "type": "object",
      "description": "WGS84 point (FHIR Location.position latitude/longitude).",
      "properties": {
        "latitude": { "type": "number", "minimum": -90, "maximum": 90 },
        "longitude": { "type": "number", "minimum": -180, "maximum": 180 }
      },
      "required": ["latitude", "longitude"],
      "additionalProperties": false
    },
    "Address": {
      "type": "object",
      "description": "A postal address (FHIR Address).",
      "properties": {
        "line": { "type": ["string", "null"], "description": "Street address line." },
        "city": { "type": ["string", "null"] },
        "state": { "type": ["string", "null"], "default": "NY" },
        "postalCode": { "type": ["string", "null"] },
        "borough": { "$ref": "#/$defs/Borough" }
      },
      "additionalProperties": false
    },
    "Geography": {
      "type": "object",
      "description": "The NYC geography spine — the join keys back to the rest of city government (BBL/BIN, community/council districts).",
      "properties": {
        "borough": { "$ref": "#/$defs/Borough" },
        "communityBoard": { "type": ["string", "integer", "null"], "description": "Community District / Board." },
        "councilDistrict": { "type": ["integer", "null"], "minimum": 1, "maximum": 51, "description": "NYC Council district." },
        "censusTract": { "type": ["string", "null"], "description": "2020 Census tract." },
        "nta": { "type": ["string", "null"], "description": "Neighborhood Tabulation Area name or code." },
        "bbl": { "type": ["string", "null"], "description": "Borough-Block-Lot." },
        "bin": { "type": ["string", "null"], "description": "Building Identification Number." }
      },
      "additionalProperties": false
    },
    "Network": {
      "type": ["string", "null"],
      "description": "The H+H care network a facility belongs to.",
      "enum": ["Acute Care", "Gotham Health", "Post-Acute / Long-Term Care", "Behavioral Health", "Correctional Health Services", "Community Care", null]
    },
    "FacilityReference": {
      "type": "object",
      "description": "A reference to an H+H facility (FHIR Location / Organization reference).",
      "properties": {
        "facilityId": { "type": "string", "description": "H+H facility identifier (maps to FHIR Location.id / Organization.id)." },
        "name": { "type": ["string", "null"], "description": "Facility display name." },
        "network": { "$ref": "#/$defs/Network" }
      },
      "required": ["facilityId"],
      "additionalProperties": false
    },
    "FhirReference": {
      "type": "object",
      "description": "A literal reference into the live Epic FHIR R4 API (epicproxypda.nychhc.org/FHIRProxy/api/FHIR/R4), when a resource exists there.",
      "properties": {
        "resourceType": { "type": "string", "description": "FHIR resource type, e.g. Location, Organization, Practitioner, Appointment, Patient." },
        "id": { "type": ["string", "null"], "description": "FHIR logical id." },
        "reference": { "type": ["string", "null"], "description": "Relative or absolute FHIR reference, e.g. 'Appointment/abc123'." }
      },
      "required": ["resourceType"],
      "additionalProperties": false
    }
  }
}
