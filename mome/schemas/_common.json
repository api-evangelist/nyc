{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://raw.githubusercontent.com/api-evangelist/nyc/main/mome/schemas/_common.json",
  "title": "MOME \u2014 Shared Definitions",
  "description": "Reusable JSON Schema definitions shared across NYC Mayor's Office of Media & Entertainment (MOME) object schemas. Reconciles fields common to the MOME Open Data datasets (data.cityofnewyork.us \u2014 Film Permits tg4x-b46p, MARCH Inspections b84a-xy2t), nyc.gov/site/mome, and the login-walled 'MOME E-Apply' film-permit portal (nyceventpermits.nyc.gov/film). Includes the NYC geography spine that the Film Permits dataset keys on (borough, community board, police precinct, ZIP).",
  "x-nyc-commons": {
    "role": "consumer",
    "source": "https://raw.githubusercontent.com/api-evangelist/nyc/main/nyc-commons/nyc-commons.json",
    "version": "0.1.0",
    "note": "Geography/identifier/address/money definitions are $ref'd from the canonical nyc-commons set; agency-specific definitions remain local."
  },
  "$defs": {
    "Borough": {
      "$ref": "https://raw.githubusercontent.com/api-evangelist/nyc/main/nyc-commons/geography.json#/$defs/Borough",
      "$comment": "Canonical definition provided by nyc-commons (geography.json#/$defs/Borough); migrated from a local definition. Back-compatible: the $defs name is unchanged, so object schemas referencing _common.json#/$defs/Borough still resolve."
    },
    "BoroughList": {
      "type": [
        "string",
        "null"
      ],
      "description": "One or more boroughs. In the Film Permits dataset ('Borough') this is a comma-separated list because a single shoot can span boroughs (e.g. 'Manhattan, Brooklyn')."
    },
    "Coordinates": {
      "$ref": "https://raw.githubusercontent.com/api-evangelist/nyc/main/nyc-commons/geography.json#/$defs/Coordinates",
      "$comment": "Canonical definition provided by nyc-commons (geography.json#/$defs/Coordinates); migrated from a local definition. Back-compatible: the $defs name is unchanged, so object schemas referencing _common.json#/$defs/Coordinates still resolve."
    },
    "Geography": {
      "$ref": "https://raw.githubusercontent.com/api-evangelist/nyc/main/nyc-commons/geography.json#/$defs/GeographySpine",
      "$comment": "Canonical definition provided by nyc-commons (geography.json#/$defs/GeographySpine); migrated from a local definition. Back-compatible: the $defs name is unchanged, so object schemas referencing _common.json#/$defs/Geography still resolve."
    },
    "ProductionCategory": {
      "type": [
        "string",
        "null"
      ],
      "description": "Production category ('Category' in Film Permits).",
      "enum": [
        "Film",
        "Television",
        "Commercial",
        "Still Photography",
        "WEB",
        "Music Video",
        "Theater",
        "Documentary",
        "Student",
        "Red Carpet/Premiere",
        "Other",
        null
      ]
    },
    "PermitEventType": {
      "type": [
        "string",
        "null"
      ],
      "description": "Permit event type ('EventType' in Film Permits).",
      "enum": [
        "Shooting Permit",
        "Theater Load in and Load Out",
        "Rigging Permit",
        "DCAS Prep/Shoot/Wrap Permit",
        "Other",
        null
      ]
    },
    "ContactInfo": {
      "type": "object",
      "description": "Applicant / production contact details (from the E-Apply intake; never published to Open Data).",
      "properties": {
        "name": {
          "type": [
            "string",
            "null"
          ]
        },
        "email": {
          "type": [
            "string",
            "null"
          ],
          "format": "email"
        },
        "phone": {
          "type": [
            "string",
            "null"
          ]
        }
      },
      "additionalProperties": false
    }
  }
}
