{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://raw.githubusercontent.com/api-evangelist/nyc/main/bronxbp/schemas/_common.json",
  "title": "Bronx Borough President — Shared Definitions",
  "description": "Reusable JSON Schema definitions shared across Office of the Bronx Borough President object schemas. Reconciles fields common to the two NYC Open Data datasets published under 'Bronx Borough President (BPBX)' (Capital Funding mdgu-ar69, Bronx Community Boards wbau-xy7g) and the brochure content on bronxboropres.nyc.gov (a Revize CMS). Includes the NYC geography spine and a Bronx community-board reference. NOTE: the schema keeps the five-borough enum because these definitions are intended to be reused across all five Borough President offices (this is the same charter role, near-identically implemented); for the Bronx office the borough is always 'Bronx'.",
  "$defs": {
    "Borough": {
      "type": "string",
      "enum": ["Manhattan", "Bronx", "Brooklyn", "Queens", "Staten Island"]
    },
    "Coordinates": {
      "type": "object",
      "description": "WGS84 point (Open Data Latitude / Longitude).",
      "properties": {
        "latitude": { "type": "number", "minimum": -90, "maximum": 90 },
        "longitude": { "type": "number", "minimum": -180, "maximum": 180 }
      },
      "required": ["latitude", "longitude"],
      "additionalProperties": false
    },
    "Geography": {
      "type": "object",
      "description": "The NYC geography spine shared across the Bronx BP datasets — the join keys back to the rest of city government.",
      "properties": {
        "borough": { "$ref": "#/$defs/Borough" },
        "communityBoard": { "type": ["string", "integer", "null"], "description": "Community District / Board ('Community Board'). In the Bronx, 1–12." },
        "councilDistrict": { "type": ["integer", "null"], "minimum": 1, "maximum": 51, "description": "NYC Council district ('Council District')." },
        "censusTract": { "type": ["string", "null"], "description": "2020 Census tract ('Census Tract (2020)')." },
        "nta": { "type": ["string", "null"], "description": "Neighborhood Tabulation Area name or code ('Neighborhood Tabulation Area (NTA) (2020)')." },
        "bbl": { "type": ["string", "null"], "description": "Borough-Block-Lot ('BBL')." },
        "bin": { "type": ["string", "null"], "description": "Building Identification Number ('BIN')." },
        "postcode": { "type": ["string", "null"], "description": "ZIP / postal code ('Postcode')." },
        "schoolDistrict": { "type": ["string", "integer", "null"], "description": "'School District'." }
      },
      "additionalProperties": false
    },
    "StateLegislativeDistricts": {
      "type": "object",
      "description": "State/federal legislative overlays carried by the Bronx BP datasets.",
      "properties": {
        "congressionalDistrict": { "type": ["string", "integer", "null"], "description": "'Congressional District'." },
        "stateSenateDistrict": { "type": ["string", "integer", "null"], "description": "'State Senate District'." },
        "stateAssemblyDistrict": { "type": ["string", "integer", "null"], "description": "'State Assembly District'." }
      },
      "additionalProperties": false
    },
    "CommunityBoardReference": {
      "type": "object",
      "description": "A reference to one of the Bronx's twelve community boards.",
      "properties": {
        "borough": { "$ref": "#/$defs/Borough" },
        "communityBoard": { "type": ["string", "integer"], "description": "Board number ('Community Board'). Bronx boards are 1–12." },
        "name": { "type": ["string", "null"], "description": "Display name, e.g. 'Bronx Community Board 6'." }
      },
      "required": ["communityBoard"],
      "additionalProperties": false
    }
  }
}
