{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://raw.githubusercontent.com/api-evangelist/nyc/main/ddc/schemas/_common.json",
  "title": "DDC — Shared Definitions",
  "description": "Reusable JSON Schema definitions shared across NYC Department of Design and Construction (DDC) object schemas. Reconciles fields common to the DDC Open Data datasets (data.cityofnewyork.us), nyc.gov/site/ddc, and the citywide vendor/procurement systems DDC solicits through (PASSPort/MOCS, City Record, Checkbook NYC). Includes the NYC geography spine and DDC's own project/contract keys (Project ID, PIN).",
  "$defs": {
    "Borough": {
      "type": "string",
      "enum": ["Manhattan", "Bronx", "Brooklyn", "Queens", "Staten Island", "Citywide"]
    },
    "Coordinates": {
      "type": "object",
      "description": "WGS84 point (Open Data LATITUDE / LONGITUDE) where a project site is geocoded.",
      "properties": {
        "latitude": { "type": "number", "minimum": -90, "maximum": 90 },
        "longitude": { "type": "number", "minimum": -180, "maximum": 180 }
      },
      "required": ["latitude", "longitude"],
      "additionalProperties": false
    },
    "Geography": {
      "type": "object",
      "description": "The NYC geography spine — the join keys back to the rest of city government. Only partially populated on DDC datasets, which are project/contract oriented rather than address oriented.",
      "properties": {
        "borough": { "$ref": "#/$defs/Borough" },
        "communityBoard": { "type": ["string", "integer", "null"], "description": "Community District / Board." },
        "councilDistrict": { "type": ["integer", "null"], "minimum": 1, "maximum": 51, "description": "NYC Council district." },
        "censusTract": { "type": ["string", "null"], "description": "2020 Census tract." },
        "nta": { "type": ["string", "null"], "description": "Neighborhood Tabulation Area name or code." },
        "bbl": { "type": ["string", "null"], "description": "Borough-Block-Lot." },
        "bin": { "type": ["string", "null"], "description": "Building Identification Number." }
      },
      "additionalProperties": false
    },
    "Division": {
      "type": ["string", "null"],
      "description": "DDC line of business ('Division') — the two capital portfolios plus support divisions.",
      "enum": ["Infrastructure", "Public Buildings", "Program Management", "Other", null]
    },
    "ProjectPhase": {
      "type": ["string", "null"],
      "description": "Where the capital project sits in the design-through-construction lifecycle ('Phase').",
      "enum": ["Scoping", "Design", "Procurement", "Construction", "Closeout", "Completed", "Other", null]
    },
    "Money": {
      "type": ["number", "null"],
      "description": "A US dollar amount ('Dollar Amount' / '$Value'). Parsed from currency-formatted Open Data strings."
    },
    "ContractReference": {
      "type": "object",
      "description": "A reference to a DDC procurement by its identifiers.",
      "properties": {
        "pin": { "type": ["string", "null"], "description": "Procurement Identification Number ('PIN') — the citywide contract key." },
        "projectId": { "type": ["string", "null"], "description": "DDC internal Project ID ('Project ID')." },
        "description": { "type": ["string", "null"], "description": "Short scope/title of the contract or project." }
      },
      "additionalProperties": false
    },
    "Firm": {
      "type": "object",
      "description": "A private-sector firm doing business with DDC — designer, engineer, construction manager, contractor, subcontractor, or allied consultant.",
      "properties": {
        "name": { "type": "string", "description": "Firm / vendor name ('SELECTED FIRM')." },
        "role": { "type": ["string", "null"], "description": "Role on the engagement (Designer, Engineer, CM, Contractor, Subcontractor, Consultant)." },
        "mwbe": { "type": ["boolean", "null"], "description": "Minority/Women-Owned Business Enterprise designation, where known." }
      },
      "required": ["name"],
      "additionalProperties": false
    }
  }
}
