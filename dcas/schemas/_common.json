{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://raw.githubusercontent.com/api-evangelist/nyc/main/dcas/schemas/_common.json",
  "title": "DCAS — Shared Definitions",
  "description": "Reusable JSON Schema definitions shared across NYC Department of Citywide Administrative Services (DCAS) object schemas. Reconciles fields common to the DCAS Open Data datasets (data.cityofnewyork.us), nyc.gov/site/dcas, and the aNNN application layer — the City Jobs portal (cityjobs.nyc.gov, formerly a127-jobs), Employee Self-Service (a127-ess.nyc.gov, PeopleSoft/NYCAPS), and the CityStore (a856-citystore.nyc.gov, Shopify). Includes the NYC geography spine and DCAS's civil-service numbering keys (Title Code, Exam Number, List Number, Agency Code).",
  "$defs": {
    "Borough": {
      "type": "string",
      "enum": ["Manhattan", "Bronx", "Brooklyn", "Queens", "Staten Island"]
    },
    "Coordinates": {
      "type": "object",
      "description": "WGS84 point (Open Data LATITUDE / LONGITUDE).",
      "properties": {
        "latitude": { "type": "number", "minimum": -90, "maximum": 90 },
        "longitude": { "type": "number", "minimum": -180, "maximum": 180 }
      },
      "required": ["latitude", "longitude"],
      "additionalProperties": false
    },
    "Geography": {
      "type": "object",
      "description": "The NYC geography spine shared across DCAS building/fleet datasets — the join keys back to the rest of city government.",
      "properties": {
        "borough": { "$ref": "#/$defs/Borough" },
        "communityBoard": { "type": ["string", "integer", "null"], "description": "Community District / Board ('COMMUNITY DISTRICT' / 'Community Board')." },
        "councilDistrict": { "type": ["integer", "string", "null"], "description": "NYC Council district ('COUNCIL DISTRICT' / 'City Council Districts')." },
        "censusTract": { "type": ["string", "null"], "description": "2020 Census tract ('CENSUS TRACT 2020' / 'Census Tract')." },
        "nta": { "type": ["string", "null"], "description": "Neighborhood Tabulation Area name or code ('NTA' / 'NEIGHBORHOOD TABULATION AREA (NTA) (2020)')." },
        "bbl": { "type": ["string", "null"], "description": "Borough-Block-Lot ('BBL')." },
        "bin": { "type": ["string", "null"], "description": "Building Identification Number ('BIN')." },
        "postcode": { "type": ["string", "null"], "description": "ZIP / postcode ('POSTCODE' / 'Postcode')." }
      },
      "additionalProperties": false
    },
    "AgencyReference": {
      "type": "object",
      "description": "A reference to a city agency by DCAS codes.",
      "properties": {
        "agencyName": { "type": ["string", "null"], "description": "Agency name ('AGENCY NAME' / 'Agency' / 'List Agency Desc')." },
        "agencyCode": { "type": ["string", "null"], "description": "Agency code ('AGENCY CODE' / 'List Agency Code')." }
      },
      "additionalProperties": false
    },
    "TitleReference": {
      "type": "object",
      "description": "A reference to a civil-service title by its Title Code — DCAS's primary personnel key.",
      "properties": {
        "titleCode": { "type": ["string", "null"], "description": "Civil-service Title Code ('Title Code' / 'TITLE CODE' / 'List Title Code')." },
        "titleDescription": { "type": ["string", "null"], "description": "Title description ('Title Description' / 'List Title Desc' / 'Civil Service Title')." },
        "assignmentLevel": { "type": ["string", "null"], "description": "Assignment level ('Assignment Level' / 'Level')." }
      },
      "additionalProperties": false
    },
    "ExamReference": {
      "type": "object",
      "description": "A reference to a civil-service examination by its Exam Number.",
      "properties": {
        "examNumber": { "type": ["string", "null"], "description": "Examination number ('Examination Number' / 'Exam No')." },
        "examTitle": { "type": ["string", "null"], "description": "Examination title ('Examination Title')." },
        "examType": {
          "type": ["string", "null"],
          "description": "Open-competitive, promotion, or qualified incumbent exam ('Open Competitive/Promotion/Qualified Incumbent Exam (QIE)').",
          "enum": ["Open Competitive", "Promotion", "Qualified Incumbent Exam", "Education & Experience", null]
        }
      },
      "additionalProperties": false
    }
  }
}
